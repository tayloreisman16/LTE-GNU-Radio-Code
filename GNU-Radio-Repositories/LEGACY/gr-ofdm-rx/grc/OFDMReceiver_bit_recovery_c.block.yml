# auto-generated by grc.converter

id: OFDMReceiver_bit_recovery_c
label: Bit Recovery QPSK
category: '[OFDMReceiver]'

parameters:
-   id: directory_name
    label: Directory Name
    dtype: string
-   id: diagnostics
    label: Diagnostics
    dtype: int

inputs:
-   domain: stream
    dtype: complex

templates:
    imports: import OFDMReceiver
    make: OFDMReceiver.bit_recovery_c(${directory_name}, ${diagnostics})

file_format: 1
