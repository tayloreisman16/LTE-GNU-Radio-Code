# auto-generated by grc.converter

id: OFDMReceiver_SynchronizeAndEstimate
label: Synchronize and Channel Estimate
category: '[OFDMReceiver]'

parameters:
-   id: case
    label: Case
    dtype: int

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: import OFDMReceiver
    make: OFDMReceiver.SynchronizeAndEstimate(${case})

file_format: 1
