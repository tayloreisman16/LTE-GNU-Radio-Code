# auto-generated by grc.converter

id: OFDMReceiver_SynchEstAndFO
label: SynchEstAndFO
category: '[OFDMReceiver]'

parameters:
-   id: case
    label: Case Number
    dtype: int
-   id: fo_range
    label: F Offset Range
    dtype: raw
-   id: directory_name
    label: Directory Path
    dtype: string
-   id: file_name_cest
    label: 'Var: Chan Est -- File Name'
    dtype: string
-   id: diagnostics
    label: Diagnostics
    dtype: int

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: import OFDMReceiver
    make: OFDMReceiver.SynchEstAndFO(${case}, ${fo_range}, ${directory_name}, ${file_name_cest},
        ${diagnostics})

file_format: 1
