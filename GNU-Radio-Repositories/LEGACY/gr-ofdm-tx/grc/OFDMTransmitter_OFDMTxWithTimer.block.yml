# auto-generated by grc.converter

id: OFDMTransmitter_OFDMTxWithTimer
label: OFDM Tx With Timer
category: '[OFDMTransmitter]'

parameters:
-   id: case
    label: Case
    dtype: int
-   id: pickle_directory
    label: Pickle Directory
    dtype: string

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: import OFDMTransmitter
    make: OFDMTransmitter.OFDMTxWithTimer(${case}, ${pickle_directory})

file_format: 1
